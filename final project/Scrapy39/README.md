

**数据库生成语句**


- A39net

  ```sql
  CREATE TABLE A39net (
    `name` VARCHAR(255) NOT NULL,
    `introduction` TEXT,
    `altname` VARCHAR(255),
    `pathogenic_site` VARCHAR(255),
    `department` VARCHAR(255),
    `population` VARCHAR(255),
    `symptom` TEXT,
    `inspect` TEXT,
    `complication` TEXT,
    `treatment` TEXT,
    `medication` TEXT,
    `cause` TEXT
  )
  ```


